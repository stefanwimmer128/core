{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run --sort --stream build"
  },
  "devDependencies": {
    "flow-bin": "0.86.0",
    "lerna": "3.4.3"
  }
}
