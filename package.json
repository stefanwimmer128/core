{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run --sort --stream build"
  },
  "devDependencies": {
    "flow-bin": "^0.82.0",
    "lerna": "^3.4.1"
  }
}
